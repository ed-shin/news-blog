# 견문록 블로그

Astro 정적 블로그. 글은 `src/content/blog/YYYY-MM-DD-슬러그.md`, main에 push하면 Cloudflare Workers가 https://gyeonmunrok.com 에 자동 배포한다.

## 편집 원칙 — 수집은 기자, 검증은 데스크

뉴스 파이프라인 자료(Google Drive `/뉴스파이프라인/`의 `daily/*/digest.md`, 분야별 수집 로그, `topic-tracker.md`)는 취재 메모다. 그대로 옮기지 말고 데스크처럼 검증한 뒤 쓴다.

1. **링크가 붙은 것만 사실로 쓴다.** 매체 이름만 있거나, 여러 링크가 한 문단에 묶여 어느 기사가 근거인지 특정할 수 없으면 싣지 않는다. 사이트 첫 화면(예: `news.hada.io`)이나 실시간 시세 페이지처럼 열 때마다 내용이 바뀌는 링크도 근거가 아니다. 개별 글·기사 주소를 찾아 쓴다.
2. **수치가 엇갈리면 같은 지표인지부터 본다.** 다른 지표(예: 산유량 vs 공급량)면 지표명과 출처를 정확히 적고, 같은 지표면 링크된 값을 쓰거나 범위로 표기한다.
3. **"미확정"은 출처 있는 보도끼리 갈린 사실에만 쓴다.** 출처 없는 사실을 미확정으로 포장해 싣지 않는다.
4. **날짜는 사건일 기준.** 브리핑(D)은 대개 전날(D-1) 사건·마감을 다루므로 사건일을 확인한다.
5. **사실과 해석을 나눈다.** 해석은 "해설:"로 표시하고, 전망·시장 확률을 사실처럼 쓰지 않는다.
6. **기사 문장을 옮기지 않는다.** 재구성해서 쓰고, 인용은 짧은 발언에 한한다.
7. **정정은 드러내고 한다.** 공개한 글을 고치면 frontmatter에 `updatedDate`와 함께 `corrections` 항목을 남긴다. 사실을 바로잡은 것뿐 아니라 형식을 바꾼 것도 적는다. 글 끝과 `/jogan/corrections/`에 그대로 보인다.
   ```yaml
   corrections:
     - date: 2026-09-20
       text: "무엇을 왜 고쳤는지 한두 문장. 독자가 이전 판을 봤더라도 알 수 있게 쓴다."
   ```
8. **사람과 기업 이름이 나오면 더 좁게 쓴다.** 실명이 등장하는 문장은 소송·분쟁의 소지가 있는 자리다.
   - 혐의·의혹·주장을 사실로 쓰지 않는다. 누가 주장했는지, 어느 기관이 조사 중인지, 상대는 뭐라고 했는지를 함께 적는다. 확인된 것과 주장된 것을 한 문장에 섞지 않는다.
   - 동기와 의도("~를 노리고", "~하려고")는 본인이 밝혔거나 보도가 그렇게 전한 경우에만 쓰고, 그때도 누가 그렇게 말했는지 적는다.
   - 평가어(무리한, 부실한, 실패한)를 이름 옆에 붙이지 않는다. 사실과 수치로 적고 판단은 독자에게 맡긴다. 해설에서도 근거를 먼저 쓰고 해석을 뒤에 붙인다.
   - 반론이 보도에 있으면 함께 싣는다. 없으면 "이에 대한 입장은 확인되지 않았다"처럼 없다는 사실만 적는다. 반론이 없다는 것을 유리하게 해석하지 않는다.
   - 개인 신상(주소, 연락처, 가족관계, 건강)은 보도에 있어도 싣지 않는다.
9. **출처는 문장 곁에 둔다.** 기업·인물이 등장하는 문장은 그 문장에 매체명과 링크를 함께 단다. 문단 끝에 한 번 몰아 달지 않는다. 통신사 보도를 국내 매체가 인용한 경우에는 "로이터가 전했다고 이투데이가 보도했다"처럼 경로를 밝힌다. 두 매체가 다르게 적었으면 어느 쪽을 썼는지 드러낸다.
10. **출처 편향을 확인한다.** 특정 매체 접근이 막혀 한 매체 비중이 크게 쏠렸을 때만 본문에 한 줄로 밝힌다. 글마다 별도 "출처와 방법" 섹션은 두지 않는다(공통 원칙은 소개 페이지에 있음).

## 글쓰기 — 한 줄로 말할 수 있게

길이보다 먼저 볼 것은 무슨 말을 하려는지가 보이느냐다. 검증을 통과했다고 다 싣지 않는다. 분량을 억지로 줄일 필요는 없지만, 할 말을 받치지 않는 문장은 맞는 사실이어도 뺀다.

- **할 말을 먼저 정한다.** 섹션마다, 1면 해설 한 갈래마다 "한 줄로 말하면"을 정하고 그 문장을 첫 문장(굵게)에 쓴다. 이어지는 문장은 그 한 줄을 받치는 사실과 그 뜻만 쓴다.
- **검증 과정은 본문에 쓰지 않는다.** "기사마다 갈렸다", "종가라고 밝힌 기사가 없다", "확인하지 못했다" 같은 문장은 desk-report에 남긴다. 본문에는 결과만 쓴다. 값이 갈리면 범위 하나로(`5.16~5.20%`), 종가가 아니면 한 단어로(`장중`) 쓴다. 보도가 갈렸다는 사실 자체가 뉴스일 때만 본문에 쓴다(사상자 수, 협상 타결 여부처럼).
- 한 문장에는 사실 하나. 숫자는 한 문장에 두세 개까지, 더 많으면 목록이나 표로 뺀다.
- 단락은 350자 안에서 끊는다. 넘으면 할 말이 둘인지, 첫 문장을 받치지 않는 사실이 섞였는지 본다.
- 1면 해설은 `lead`가 결론이고 `text`는 그 결론을 받치는 두세 문장이다(100~180자). 깊이는 길이가 아니라 "왜 그런지"를 한 번 짚는 데서 나온다.
- 처음 나오는 용어와 약어는 짧게 풀어 쓴다(점도표, 근원 PCE, 10년물 금리, 순매수, 해상 환적 등). 풀이는 일반 상식 수준까지만 하고, 뉴스 사실은 여전히 링크가 있어야 한다.
- 앞선 날에서 이어지는 사건은 배경을 한 줄 깔고 기획·지난 일일 글로 내부 링크를 건다.
- 해설은 "그래서 무엇이 달라지나"를 평이한 말로 쓴다.

`scripts/check_post.py`가 긴 단락(350자), 긴 1면 해설(180자), 검증 과정 문구에 경고를 낸다. 경고가 나오면 위 원칙대로 고친다. 보도가 갈린 것이 뉴스인 경우처럼 그대로 둘 까닭이 있으면 desk-report에 적는다.

금융 콘텐츠이므로 "투자 권유 아님" 고지를 유지한다(레이아웃 footer와 포스트 하단에 자동 포함).

## 사이트 구조

- `/` 견문록 홈, `/jogan/` 오늘의 1면(가장 최근 일일), `/jogan/today/2026-09-19/` 그날의 1면(날짜별로 보존), `/jogan/flow/` 흐름, `/jogan/all/` 전체(`?type=daily`, `?tag=금리`로 필터).
- 조간 글 주소는 종류별로 나뉜다(`src/lib/posts.ts`의 `postUrl`). 글 파일 이름은 그대로 `src/content/blog/`에 둔다.
  - 일일 `/jogan/daily/2026-09-19/` ← `2026-09-19-daily.md`(게시일)
  - 주간 흐름 `/jogan/weekly/2026-09-14/` ← `2026-09-19-weekly.md`(`period.from`, 그 주 월요일)
  - 기획 `/jogan/feature/iran-oil-100/` ← `2026-09-16-iran-oil-100.md`(파일 이름에서 앞 날짜를 뺀 것)
  - 종류 태그가 없는 글만 `/blog/파일이름/`. 글 속 내부 링크도 이 주소로 건다.
  - 이미 공개된 주소를 바꾸면 `public/_redirects`에 301 줄을 더한다(Cloudflare가 처리).
- 날짜별 1면(`/jogan/today/날짜/`)은 검색 색인에 싣지 않는다. `noindex, follow`를 걸고 사이트맵에서도 거른다(`astro.config.mjs`의 `filter`). `/jogan/`과 본문이 99.9% 같고 지난 날짜 것은 그날 일일 글의 요약이라, 서치 콘솔이 "크롤링됨 - 색인 생성되지 않음"으로 남겼다(2026-09-25 조치). 사람에게는 그대로 보이고 일일 글로 가는 링크도 살아 있다. 새 일일 글에는 자동으로 적용되니 발행 때 따로 할 일은 없다. 사이트맵에 이 주소를 되돌리지 않는다.
- 기획 글을 새로 내면 서치 콘솔에서 "색인 생성 요청"을 눌러 달라고 사용자에게 알린다. 오래 검색에 남는 글이라 먼저 들어가는 편이 낫고, 요청은 하루 건수 제한이 있다. 일일 글은 사이트맵으로 따라오므로 건건이 요청하지 않는다.
- 화면 용어는 글 종류 이름(일일 / 주간 흐름 / 기획)을 그대로 쓴다. "브리핑" 대신 "일일"(예: 지난 일일, 일일 전체 보기).
- 조간 글은 `tags` 첫머리에 종류 태그 하나(`일일` / `주간` / `기획`)를 넣고, 나머지는 주제 태그(`금리` `시장` `에너지` `지정학` `테크` `부동산`)다. 종류 태그가 없는 글은 조간 밖의 글로 본다.
- 일일 글은 frontmatter `front`에 1면 데이터(헤드라인, 해설 3갈래, 주요 지표, 데스크별 2건+요약)를 둔다. 형식은 `src/content.config.ts`와 기존 일일 글을 따른다. 1면에 가장 최근의 `front` 있는 일일 글이 걸린다.
- 1면 주요 지표는 코스피·원/달러·S&P 500·미 10년물·브렌트·금 여섯 개로 고정한다(기준금리는 바뀐 날만 더한다). 모두 종가이고 유가·금은 선물 정산가다. 값을 아예 구하지 못한 지표만 "—"로 둔다. 휴장(추석·설·공휴일)으로 새 마감이 없으면 직전 거래일 종가를 채우고 등락을 비우며 `asOf`에 휴장과 기준일을 밝힌다. 값은 확인됐는데 종가가 아니거나 기사마다 갈리면 기준을 밝혀 싣는다(예: `장중 5.22%`, `5.16~5.20%`). 본문이나 헤드라인에 있는 숫자가 1면에서 빈칸이 되지 않게 한다. 본문 "오늘의 숫자" 표에는 WTI·나스닥 등을 더 넣어도 된다.
- 일일 글은 `## 다음에 볼 것`으로 끝낸다. 두세 개만 쓴다. 매일 읽는 사람에게는 이 자리가 가장 쓸모 있다.
  - 각 항목은 본문에 이미 실린, 링크로 뒷받침된 사실에서 출발한다. 여기서 새 사실을 처음 꺼내지 않는다.
  - 전망("오를 것이다")이 아니라 확인 방법("무엇을 보면 알 수 있다")으로 쓴다. 시장 확률과 예측은 쓰지 않는다.
  - 다음 날 글에서는 전날 항목이 어떻게 됐는지 해당 섹션 본문에서 이어 쓴다. 확인되면 사실로 싣고, 확인되지 않았으면 그대로 두거나 뺀다.
  - 수집기 다이제스트의 "관찰 포인트"를 소재로 삼되 그대로 옮기지 않는다. 근거가 없는 항목은 뺀다.
  - 항목 하나는 세 갈래로 쓴다. ①지금까지 확인된 것과 확인되지 않은 것, ②언제 무엇을 보면 되는지, ③그 결과가 어느 쪽으로 갈리는지. 개수가 적다고 문장까지 줄이지 않는다. 본문과 같은 밀도로 쓴다.
  - 세 갈래는 줄을 나눠 쓴다. 굵은 첫 줄(무엇을 볼지) 아래 두 칸 들여 쓴 줄 셋이고, 갈래마다 한두 문장이다. 한 덩어리로 이어 쓰면 밀도가 아니라 벽이 된다.
  - 전날 항목의 결과를 이어 쓸 때는 한 문장으로 쓴다. "어제 이 글이 물은" 같은 말로 과정을 늘어놓지 않는다.
  - 장 마감 시각처럼 상식에 속하는 것은 그냥 쓴다. 검사기 경고를 피하려고 문장을 흐리게 만들지 않는다.
- 본문 섹션 순서는 데스크 순서(금리 → 시장 → 에너지 → 지정학 → IT · 테크 → 그 외)에 맞춘다. 데스크 이름은 `## 금리`, `## 에너지`, `## 지정학`, `## IT`, `## 그 외`로 시작하는 섹션으로 연결된다.
- 기획·주간 흐름 글은 frontmatter에 `period: { from: YYYY-MM-DD, to: YYYY-MM-DD }`로 다루는 기간을 반드시 적는다(없으면 빌드 실패). 1면·흐름·전체·홈·글 머리에 "8월 24일 ~ 9월 13일"로 표시된다. 주간 흐름도 적으면 같이 표시된다.
- 섹션 제목은 `## 라벨 — 제목` 형식이면 금색 라벨과 제목으로 나뉘어 보이고, 차례(오른쪽, 작은 화면은 칩)에도 라벨로 나온다. 표에서 `+`/`-`로 시작하는 숫자 칸은 등락 색이 입혀진다.
- 흐름 페이지의 "추적 중인 흐름"은 `src/data/tracking.json`. 실린 글로 뒷받침되는 토픽만 올린다.

## 매일 아침 자동 발행

수집기(08:30)와 편집자(09:00)가 Google Drive `/뉴스파이프라인/daily/D/`에 `post.md`, `desk-report.md`, `tracking.json`을 만들어 둔다. 그 뒤 데스크가 검증해서 싣는 일까지 예약 작업으로 돈다. 순서는 이렇다.

1. Drive에서 그날 폴더를 찾는다. `post.md`가 없으면 멈추고 보고한다. 이미 `src/content/blog/D-daily.md`가 있으면 덮어쓰지 않는다.
2. **원문 대조.** 본문에 실린 외부 링크를 전부 열어 숫자·따옴표 안 인용문·날짜·출처를 기사와 맞춰 본다. 링크가 많으므로 분야별로 나눠 병렬로 돌린다. 도구가 요약을 돌려주므로 "해당 문장을 원문 그대로 인용해 달라"고 요청해 대조한다.
3. 위의 편집 원칙대로 고친다. 근거 없는 서술은 빼고, 사진 캡션·메타 설명처럼 본문이 아닌 곳에서 온 사실은 싣지 않는다. 기사에 없는 선후 관계나 시점을 지어내지 않는다.
4. 지난 글로 거는 내부 링크는 앵커까지 실제로 있는지 확인한다. 지난 글을 고치면 `#numbers` 같은 자리가 사라질 수 있다.
5. `src/content/blog/D-daily.md`와 `src/data/tracking.json`에 반영하고 검사한다.
   ```bash
   python3 scripts/check_post.py src/content/blog/D-daily.md src/data/tracking.json
   npm run build && python3 scripts/linkcheck.py
   ```
   오류는 모두 고치고 경고는 읽어 보고 판단한다.
6. 작성자 `barney <jp.shin.kor@gmail.com>`로 커밋하고 main에 푸시한다. 커밋 메시지에 무엇을 왜 고쳤는지 적는다.
7. 배포를 확인한다. **이번에 바뀐 문구로 확인한다.** 주소가 200을 준다는 것만으로는 옛 페이지일 수 있고, 엣지 캐시 때문에 이전 내용이 잠시 더 보인다.
8. 무엇을 고쳤는지, 무엇을 싣지 않았는지 보고한다. 이해 충돌(Anthropic·Claude)에 해당하는 항목은 실었든 뺐든 보고에 밝힌다.

### 일요일·월요일판

미국 시장이 주말에 쉬고 선물·외환은 한국 시간 월요일 새벽에야 다시 열려 장중 값뿐이다. 그래서 일요일판과 월요일판에는 새 마감이 없고, 토요일판이 실은 금요일 마감을 그대로 반복하게 된다.

- `## 오늘의 숫자` 표를 만들지 않는다. 그 자리에 직전 거래일 안내 한 줄만 둔다.
- 1면 지표 여섯 개는 값을 채우되 `change`는 비운다. 여섯 행 모두 비우면 1면 표에서 등락 칸이 통째로 사라진다. `markets.asOf`에 "직전 거래일"을 밝힌다.
- 금리·시장 섹션에 마감 수치를 다시 나열하지 않는다. 주말 사이 새로 나온 것만 쓰고, 쓸 것이 없으면 섹션을 만들지 않는다.
- 헤드라인은 주말 사이 새 소식에서 먼저 고른다. 전날 1면과 같은 헤드라인이 되지 않게 한다.

### 대조에서 자주 걸리는 것

지금까지 실제로 나온 오류들이다. 같은 유형을 먼저 찾는다.

- 예고를 결과로 바꿔 씀("회담한다"를 "회담했다"로).
- 근거 기사에 없는 관측·분석이 사실처럼 들어감.
- 두 통신사를 한 문단에 묶어 어느 보도가 근거인지 불명확함.
- 본문이 아니라 사진 캡션이나 메타 설명에만 있는 내용을 본문 사실로 씀.
- 기사에 없는 선후 관계("채택 전", "직후")를 입력 시각 비교로 추론해 씀.
- 선물과 현물, 계약 월물, 직함처럼 원문이 구분했거나 구분하지 않은 것을 임의로 바꿈.
- 값은 있는데 기준 날짜가 없는 수치를 해설의 근거로 씀.
- 주차 표기를 날짜로 옮겨 적음(부동산 기사에 잦다).
- 따옴표 안 문장이 원문과 다름. 인용은 글자 단위로 대조한다.

## 개발 메모

- `scripts/check_post.py`를 고치면 편집자 스킬(`jogan-editor`)의 `scripts/check_post.py`도 같은 판으로 바꾼다. 편집 단계는 스킬 사본으로, 발행 단계는 이 저장소 판으로 검사한다. 스킬은 클라우드에서 동기화되므로 로컬 사본만 고치면 다음 동기화 때 덮인다. 스킬 원본을 올려야 유지된다(2026-09-26, 스킬 사본이 주말판 규칙도 없는 옛 판이었던 것을 발견).

- `astro.config.mjs`의 마크다운 변환 규칙(remark/rehype)을 바꾼 뒤에는 `rm -rf node_modules/.astro .astro/data-store.json` 후 빌드한다. 변환 결과가 캐시돼 있어 그냥 빌드하면 반영되지 않는다.
